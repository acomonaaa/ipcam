# 理论笔记：嵌入式 Linux 系统构建与摄像头架构

整理自 i.MX6ULL（正点原子 EMMC 14x14）rootfs 搭建、NFS 网络调试、ipcam 摄像头 bring-up 全过程。
配套排查记录见《排查记录-摄像头video1未注册.md》。

---

## 一、根文件系统为什么存在

内核（zImage）只是操作系统核心。它启动的最后一步是挂载根文件系统，然后运行里面的**第一个用户态进程** `/sbin/init`（PID=1）。从这一刻起，"内核"才变成"操作系统"。挂载失败即 `Kernel panic: VFS: Unable to mount root fs`。

rootfs 必须包含：

| 内容 | 作用 |
|---|---|
| `/sbin/init` | 第一个进程，一切后续动作的总调度 |
| `bin`/`sbin`/`usr/bin` 命令 | shell 可用的工具集 |
| `/lib`、`/usr/lib` 动态库 | 应用运行时链接的 libc 等 |
| `/etc` 配置文件 | init 行为、挂载表、启动脚本 |
| `/dev` 设备节点 | 访问硬件的入口 |

---

## 二、busybox 的三个角色

1. **命令集**：一个二进制集成几百个命令，安装时用软链接伪装成 `ls`/`cp`/`ps`……解决嵌入式空间问题。
2. **init 程序**：`linuxrc`/`sbin/init` 就是 busybox 的马甲。内核挂载 rootfs 后执行的第一个进程就是它。
3. **mdev**：配合内核热插拔机制，在 `/dev` 下自动创建设备节点。

### 关键配置项的含义

| 配置 | 含义 | 为什么这么选 |
|---|---|---|
| `CONFIG_STATIC`（取消） | 动态编译 | 体积小，代价是运行时需要 `/lib` 动态库 |
| `CONFIG_MODPROBE_SMALL`（取消） | 完整版 modutils | 为加载 `.ko` 驱动模块（insmod/modprobe/depmod）做准备 |
| `CONFIG_UNICODE_SUPPORT` | 中文显示 | 需配套改 `libbb/printable_string.c`、`libbb/unicode.c`：放开"字符 ≥ 0x7f 替换为 ?"的限制（ASCII 只有 0~0x7f，默认逻辑会把中文干掉） |

### modprobe 的路径约定

busybox modprobe 严格按 `/lib/modules/$(uname -r)/` 查找模块，目录名必须与内核版本号（vermagic，如 `4.1.15-ge48931b1`）完全一致，且目录里要有 `modules.dep`（`make modules_install` 生成）。注意 tar 包顶层若直接是版本号目录，解压目标是 `/lib/modules/` 而不是 `/lib/`。

---

## 三、动态库与动态链接器

交叉编译出的应用（`file` 显示 `dynamically linked, interpreter /lib/ld-linux-armhf.so.3`）运行时分两步：

1. **内核**加载 interpreter（`/lib/ld-linux-armhf.so.3`，动态链接器/loader）；
2. **loader** 按程序的依赖清单加载 `libc.so.6` 等，补全符号地址。

要点：

- 库必须与**同一个交叉编译器的 sysroot** 配套，版本错配直接跑不起来；
- `ld-linux-armhf.so.3` 原本是 24 字节软链接，教程要求删掉重拷为真实文件（约 724392B），确保 loader 实实在在存在于 rootfs，规避早期启动阶段符号解析问题。

---

## 四、NFS 根文件系统

### 原理

板子的 rootfs 不实际存在，启动时通过网络把 Ubuntu 上的一个目录挂为板子的 `/`。板上每个文件操作都是一次网络请求。开发收益：Ubuntu 侧编译/修改，板上立即可见，无下载无烧写。

### bootargs 参数链

```
root=/dev/nfs
nfsroot=<server-ip>:<root-dir>[,<nfs-options>]
ip=<client-ip>:<server-ip>:<gw-ip>:<netmask>:<hostname>:<device>:<autoconf>
```

### 踩过的坑：NFS 版本协商

- 内核 4.1.15 的 nfsroot **默认用 NFSv2**；
- 新版 Ubuntu 的 NFS 服务器已移除 v2（`rpcinfo -p` 只见 v3/v4）；
- 结果：内核以 v2 反复超时约 96 秒后 panic；
- **修复：`nfsroot=...,v3,proto=tcp` 显式指定 v3。**

---

## 五、完整启动链路

```
uboot
 ├─ bootcmd：倒计时结束自动执行（tftp 内核/dtb → bootz）
 └─ bootargs：传给内核的参数（root/nfsroot/ip/console）
        ▼
kernel
 ├─ 解析 bootargs，IP-Config 配网
 ├─ NFSv3 挂载 rootfs
 └─ 执行 /sbin/init（busybox）
        ▼
init 读 /etc/inittab
 ├─ ::sysinit:/etc/init.d/rcS     → 初始化脚本
 └─ console::askfirst:-/bin/sh    → 串口 shell
        ▼
rcS
 ├─ mount -a     → 按 /etc/fstab 挂 proc/tmpfs/sysfs
 ├─ mdev -s      → 填充 /dev
 └─ modprobe ... → 加载驱动模块
        ▼
/ # shell
```

---

## 六、交叉编译工具的三层关系

**gcc 是唯一的编译器**，其余都是"管理 gcc 的工具"：

| 层 | 工具 | 角色 | 实例 |
|---|---|---|---|
| 单文件 | `arm-linux-gnueabihf-gcc hello.c -o hello` | gcc 本尊 | 书上 hello |
| 多文件工程 | make + Makefile | 把"每条 gcc 命令+依赖"写成规则，自动判断重编 | busybox、uboot、ipcam |
| 大型库 | cmake | 跨平台库的 Makefile 生成器 | libjpeg-turbo |

注意：**第三方库本身也是 C 代码，必须交叉编译成 ARM 版**，不能拿 Ubuntu x86 的库给板子链接。

### 可选依赖与 stub 模式

ipcam 对 libjpeg-turbo 的处理：提供接口相同的空壳（`ipcam_encode_stub.c`），链接通过、功能为空。先通"采集+显示"链路，后补编码——嵌入式 bring-up 常用策略。

---

## 七、设备树与驱动匹配机制

### 三种匹配途径

1. **of_match_table**：驱动声明 `compatible`，与 DT 节点 `compatible` 比对（最常用）；
2. **i2c_device_id / platform_device_id 表**：按客户端名匹配。DT 派生的 i2c client 名 = compatible 逗号后的后半段（`"ovti,ov5640"` → `"ov5640"`）；
3. **alias**：`modinfo` 显示的 `alias: i2c:ov564x` 等，用于热插拔自动 modprobe。

匹配失败的典型表现：模块加载成功（lsmod 可见）、设备节点存在（sysfs 有）、但**零探测日志**。

### 查证据的手段

- `strings xxx.ko | grep ...`：驱动期望的 compatible/ID 名是明文；无 `of:N*T*C...` alias 说明没有 of_match_table；
- `strings xxx.dtb | grep ...`：dtb 中字符串是明文，可确认节点与 compatible；
- `dtc -I dtb -O dts` 反编译可看完整节点（重编译时的 warning 属正常）；
- `cat /sys/bus/i2c/devices/*/uevent`：看内核实际派生的 MODALIAS；
- `ls -l /sys/bus/platform/drivers/<驱动名>/`：有设备链接 = probe 成功绑定。

### probe 失败与内核 oops

若 probe 内部崩溃（如解引用未初始化指针），dmesg 出现 `Code: ...` + `---[ end trace ... ]---`，末尾带括号的指令即出错指令（如 `str r4, [r3, #52]` 为野指针写）。probe 崩溃后后续初始化（如 video_register_device）永不执行，且**必须重启**才能恢复干净现场。防御性教训：调用返回值（尤其 `init_camera_struct` 这类多失败点函数）必须检查。

---

## 八、i.MX6ULL 摄像头架构（本次问题核心）

### 两条互不兼容的驱动链

| | i.MX6Q（旧，IPU 架构） | i.MX6ULL（现用，CSI 架构） |
|---|---|---|
| 硬件 | 有 IPU（图像处理单元） | **无 IPU**，只有 CSI + PxP |
| 采集主设备 | `mxc_v4l2_capture.ko`（匹配 `fsl,imx6q-v4l2-capture` 虚拟节点，内部调 `ipu_get_soc()`） | `mx6s_capture.ko`（匹配 `fsl,imx6ul-csi`） |
| 传感器驱动 | `ov5640_camera_int.ko`（I2C ID `ov564x`） | `ov5640_camera.ko`（I2C ID `ov5640`） |
| DT 写法 | 根节点虚拟 `v4l2_cap_0 { compatible = "fsl,imx6q-v4l2-capture"; ipu_id; csi_id; ... }` | 真实节点 `csi@021c4000 { compatible = "fsl,imx6ul-csi","fsl,imx6s-csi" }` + 摄像头节点 port/endpoint 图 |

在 6ULL 上加载 6Q 链路：`ipu_get_soc()` 失败 → probe 野指针 oops → `/dev/videoX` 永不出现。

### 各硬件模块的角色

- **CSI（Camera Serial Interface，此处走 DVP 并口）**：接收 OV5640 的并行数据，DMA 到内存；
- **PxP**：图像加速器（mem2mem 搬运/缩放），注册为 `/dev/video0`，**没有采集能力**；
- **MIPI CSI2**：另一路摄像头接口（本板 DVP 摄像头不用）。

### /dev/videoX 的注册流程

```
modprobe mx6s_capture          → 平台驱动匹配 csi@021c4000 → probe
modprobe ov5640_camera         → i2c 驱动匹配 ov5640@3c（I2C ID "ov5640"）
                               → 读芯片 ID 成功 → "camera ov5640 is found"
mx6s_capture 与 sensor subdev  → 通过 DT port/endpoint 图关联
                               → video_register_device → /dev/video1
```

判读节点**不能只凭编号**（PxP 先注册占 video0，采集通常排 video1，但顺序非契约），要以 `cat /sys/class/video4linux/video*/name` 和 V4L2 capability 为准。

### 模块必须与内核同源

`.ko` 与 zImage 必须来自**同一份源码、同一份配置、同一版本**编译（vermagic + 符号 CRC 校验）。混用不同 BSP 的 dtb/模块/内核，就会出现本次"dtb 写 A 架构、模块是 B 架构"的错配。

---

## 九、V4L2 基础概念（ipcam 采集依赖的框架）

- **V4L2（Video4Linux2）**：Linux 视频设备框架。应用通过 `/dev/videoX` + ioctl（`QUERYCAP`/`S_FMT`/`REQBUFS`/`QBUF`/`DQBUF`/`STREAMON`）采集。
- **capability**：每个 video 节点注册时声明自己能干什么。`V4L2_CAP_VIDEO_CAPTURE`（采集）、`V4L2_CAP_STREAMING`（流式 IO）。ipcam 打开节点前先 `QUERYCAP` 检查——PxP 节点没有 capture 能力，所以被正确拒绝。
- **subdev**：传感器等"从设备"抽象，注册为 v4l2 subdev 后由主设备（CSI 采集驱动）调度。与 mdev/int_device 等旧框架的区别正是 6Q/6ULL 两代驱动链的差异之一。
- **格式协商**：应用 `S_FMT` 请求四字符码（如 YUYV/UYVY/RGB565），驱动按能力协商；不一致时 capture 启动失败并打印期望/实际值——ipcam 的 `IPCAM_CAP_PIXFMT` 配置即为此设计。

---

## 十、经验教训汇总

1. **先查"驱动链架构"再查"配置细节"**：现象相似（节点不出现），根因可能在架构层（6Q vs 6ULL），逐个补属性是南辕北辙。
2. **以出厂/同源源码为唯一基准**：DT 写法、Kconfig、驱动 ID 表都应从同一 BSP 源码树核对，不凭通用 V4L2 经验硬推。
3. **`strings` + `modinfo` + `dtc` 三板斧**：不依赖内核源码树也能完成大部分驱动-DTB 配对诊断。
4. **模块化驱动的排障顺序**：lsmod（加载了吗）→ sysfs（绑定了吗）→ dmesg 全量（探测说了什么）→ oops Code 段（死在哪条指令）。
5. **probe 崩溃后必须重启**再测，脏现场会掩盖真实问题。
6. **bootargs/nfsroot 这类"教程没写的隐含默认值"**（NFSv2 默认、模块路径约定）是迁移到新环境时最先出问题的地方。
