# 变更记录模板

> 仅用于**代码类**修改。纯文档变更不要新建本类文件。  
> 复制本文件为 `YYYY-MM-DD-简短中文标题.md` 后填写。  
> 删除本提示块。时间必须为 **UTC+8（北京时间）**。  
> 链接一律使用相对路径，例如 `[ipcam_param.c](../src/core/ipcam_param.c)`。

## 元信息

| 项 | 内容 |
| --- | --- |
| 修改时间 | YYYY-MM-DD HH:MM:SS（UTC+8） |
| 作者 / Agent | |
| 关联提交 | （约定式提交标题或 hash，未提交则写「待提交」） |
| 影响模块 | 如 param / encode / stream / net4g / build / docs |

## 问题现象

（写清：在什么环境、什么操作下、观察到什么错误或错误行为。尽量可复现。）

1. …
2. …

## 问题分析

（写清：排查步骤、对照代码/日志/手册的推理过程，排除了哪些可能原因。）

1. …
2. …

相关代码 / 配置（相对路径链接）：

- [`路径`](../相对路径)

## 问题根因

（一到数段，明确「根因是什么」，避免只写表象。）

## 修改方案

（写清改了什么、为什么这样改、有无替代方案及取舍。）

### 变更文件列表

- [`文件`](../相对路径) — 简述改动点

### 关键改动说明

（可引用函数名、配置项；需要时用相对路径链到具体文件。）

### 中文注释说明（代码类变更必填）

（列出本次在哪些文件/函数补充了中文注释；若为纯文档变更可写「不适用」。）

**中文注释**：列出本次在哪些位置补充了详细中文注释（相对路径链接）；若为纯文档变更可写「无代码注释」。

## 预期结果

（写清修改后应有的可验证行为，以及建议的验证步骤。）

1. …
2. …

## 备注（可选）

- 风险、回滚方式、后续待办等
